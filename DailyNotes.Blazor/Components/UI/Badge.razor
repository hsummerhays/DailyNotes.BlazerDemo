<span class="badge badge-@Type.ToString().ToLower()">
    @ChildContent
</span>

@code {
    public enum BadgeType { Primary, Success, Warning, Danger }
    [Parameter] public BadgeType Type { get; set; } = BadgeType.Primary;
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
